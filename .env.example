# Public site identifier for Cloudflare Web Analytics, not a credential — it ships
# inside the page HTML either way. Get it from: Cloudflare dashboard → Web Analytics → Manage site.
PUBLIC_CF_BEACON_TOKEN=
